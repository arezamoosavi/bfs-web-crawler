version: "3"

services:
  crawler:
    build:
      context: .
      dockerfile: Dockerfile
#    volumes:
#      - ./:/opt/project
#    ports:
#      - 8080:8080
    restart: unless-stopped
#    command: sleep infinity

